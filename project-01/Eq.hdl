
/**
 * Eq gate: 
 * out = 1 if (a == b)
 *       0 otherwise
 */

CHIP Eq {
    IN a, b;
    OUT out;

    PARTS:
    // Put your code here:
    And(a=a,b=b,out=out1);
    Nand(a=a,b=b,out=out2);
    Or(a=out1,b=out2,out=out);
}
